<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/iv_character_item"
        android:layout_width="200dp"
        android:layout_height="300dp"
        android:layout_marginHorizontal="5dp"
        android:layout_marginVertical="8dp"
        android:scaleType="fitXY"
        android:src="@drawable/rick"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:shapeAppearanceOverlay="@style/Corner"
        app:strokeColor="@color/sky_blue"
        app:strokeWidth="5dp" />

    <TextView
        android:id="@+id/tv_characters_main"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:background="@color/sky_blue"
        android:textAlignment="center"
        android:textColor="@color/black"
        android:textSize="20sp"
        android:textStyle="bold"
        android:alpha="0.8"
        app:layout_constraintBottom_toBottomOf="@+id/iv_character_item"
        app:layout_constraintEnd_toEndOf="@+id/iv_character_item"
        app:layout_constraintStart_toStartOf="@+id/iv_character_item"
        tools:text="Rick" />

</androidx.constraintlayout.widget.ConstraintLayout>